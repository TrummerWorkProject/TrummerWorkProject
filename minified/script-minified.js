document.addEventListener('DOMContentLoaded',function(){initSliders();function initSliders(){initBasicSlider();initDotsSlider();initFadeSlider();initZoomSlider();initThumbsSlider()}
function initBasicSlider(){const sliders=document.querySelectorAll('.slider-basic');sliders.forEach(slidercontenedor=>{const slider=slidercontenedor.querySelector('.slider-track');const slides=slider.querySelectorAll('.slide');const prevButton=slidercontenedor.querySelector('.slider-prev');const nextButton=slidercontenedor.querySelector('.slider-next');let currentIndex=0;let intervalId;const slideInterval=5000;slider.style.width=`${slides.length * 100}%`;slides.forEach(slide=>{slide.style.width=`${100 / slides.length}%`});function showSlide(index){slider.style.transform=`translateX(-${index * (100 / slides.length)}%)`;currentIndex=index}
function nextSlide(){currentIndex=(currentIndex+1)%slides.length;showSlide(currentIndex)}
function prevSlide(){currentIndex=(currentIndex-1+slides.length)%slides.length;showSlide(currentIndex)}
function startAutoSlide(){if(intervalId)clearInterval(intervalId);intervalId=setInterval(nextSlide,slideInterval)}
function stopAutoSlide(){clearInterval(intervalId)}
if(prevButton){prevButton.addEventListener('click',()=>{prevSlide();stopAutoSlide();startAutoSlide()})}
if(nextButton){nextButton.addEventListener('click',()=>{nextSlide();stopAutoSlide();startAutoSlide()})}
slidercontenedor.addEventListener('mouseenter',stopAutoSlide);slidercontenedor.addEventListener('mouseleave',startAutoSlide);let touchStartX=0;let touchEndX=0;slidercontenedor.addEventListener('touchstart',function(e){touchStartX=e.changedTouches[0].screenX;stopAutoSlide()},{passive:!0});slidercontenedor.addEventListener('touchend',function(e){touchEndX=e.changedTouches[0].screenX;handleSwipe();startAutoSlide()},{passive:!0});function handleSwipe(){const swipeThreshold=50;if(touchEndX<touchStartX-swipeThreshold){nextSlide()}else if(touchEndX>touchStartX+swipeThreshold){prevSlide()}}
showSlide(0);startAutoSlide()})}
function initDotsSlider(){const sliders=document.querySelectorAll('.slider-dots');sliders.forEach(slidercontenedor=>{const slider=slidercontenedor.querySelector('.slider-track');const slides=slider.querySelectorAll('.slide');const prevButton=slidercontenedor.querySelector('.slider-prev');const nextButton=slidercontenedor.querySelector('.slider-next');const dotscontenedor=slidercontenedor.querySelector('.slider-dots-contenedor');let currentIndex=0;let intervalId;const slideInterval=10000;slider.style.width=`${slides.length * 100}%`;slides.forEach(slide=>{slide.style.width=`${100 / slides.length}%`});dotscontenedor.innerHTML='';slides.forEach((_,index)=>{const dot=document.createElement('div');dot.classList.add('dot');if(index===0)dot.classList.add('active');dot.addEventListener('click',()=>{showSlide(index);stopAutoSlide();startAutoSlide()});dotscontenedor.appendChild(dot)});const dots=dotscontenedor.querySelectorAll('.dot');function showSlide(index){slider.style.transform=`translateX(-${index * (100 / slides.length)}%)`;dots.forEach((dot,i)=>{if(i===index){dot.classList.add('active')}else{dot.classList.remove('active')}});currentIndex=index}
function nextSlide(){currentIndex=(currentIndex+1)%slides.length;showSlide(currentIndex)}
function prevSlide(){currentIndex=(currentIndex-1+slides.length)%slides.length;showSlide(currentIndex)}
function startAutoSlide(){if(intervalId)clearInterval(intervalId);intervalId=setInterval(nextSlide,slideInterval)}
function stopAutoSlide(){clearInterval(intervalId)}
if(prevButton){prevButton.addEventListener('click',()=>{prevSlide();stopAutoSlide();startAutoSlide()})}
if(nextButton){nextButton.addEventListener('click',()=>{nextSlide();stopAutoSlide();startAutoSlide()})}
slidercontenedor.addEventListener('mouseenter',stopAutoSlide);slidercontenedor.addEventListener('mouseleave',startAutoSlide);let touchStartX=0;let touchEndX=0;slidercontenedor.addEventListener('touchstart',function(e){touchStartX=e.changedTouches[0].screenX;stopAutoSlide()},{passive:!0});slidercontenedor.addEventListener('touchend',function(e){touchEndX=e.changedTouches[0].screenX;handleSwipe();startAutoSlide()},{passive:!0});function handleSwipe(){const swipeThreshold=50;if(touchEndX<touchStartX-swipeThreshold){nextSlide()}else if(touchEndX>touchStartX+swipeThreshold){prevSlide()}}
showSlide(0);startAutoSlide()})}
function initFadeSlider(){const sliders=document.querySelectorAll('.slider-fade');sliders.forEach(slidercontenedor=>{const slider=slidercontenedor.querySelector('.slider-track');const slides=slider.querySelectorAll('.slide');const prevButton=slidercontenedor.querySelector('.slider-prev');const nextButton=slidercontenedor.querySelector('.slider-next');let currentIndex=0;let intervalId;const slideInterval=5000;slides.forEach((slide,index)=>{if(index===0){slide.classList.add('active')}else{slide.classList.remove('active')}});function showSlide(index){slides.forEach((slide,i)=>{if(i===index){slide.classList.add('active')}else{slide.classList.remove('active')}});currentIndex=index}
function nextSlide(){currentIndex=(currentIndex+1)%slides.length;showSlide(currentIndex)}
function prevSlide(){currentIndex=(currentIndex-1+slides.length)%slides.length;showSlide(currentIndex)}
function startAutoSlide(){if(intervalId)clearInterval(intervalId);intervalId=setInterval(nextSlide,slideInterval)}
function stopAutoSlide(){clearInterval(intervalId)}
if(prevButton){prevButton.addEventListener('click',()=>{prevSlide();stopAutoSlide();startAutoSlide()})}
if(nextButton){nextButton.addEventListener('click',()=>{nextSlide();stopAutoSlide();startAutoSlide()})}
slidercontenedor.addEventListener('mouseenter',stopAutoSlide);slidercontenedor.addEventListener('mouseleave',startAutoSlide);showSlide(0);startAutoSlide()})}
function initZoomSlider(){const sliders=document.querySelectorAll('.slider-zoom');sliders.forEach(slidercontenedor=>{const slider=slidercontenedor.querySelector('.slider-track');const slides=slider.querySelectorAll('.slide');const prevButton=slidercontenedor.querySelector('.slider-prev');const nextButton=slidercontenedor.querySelector('.slider-next');slides.forEach(slide=>{if(!slide.querySelector('.zoom-contenedor')){const img=slide.querySelector('img');if(img){const zoomcontenedor=document.createElement('div');zoomcontenedor.classList.add('zoom-contenedor');const imgClone=img.cloneNode(!0);img.parentNode.removeChild(img);zoomcontenedor.appendChild(imgClone);slide.insertBefore(zoomcontenedor,slide.firstChild)}}});let currentIndex=0;let intervalId;const slideInterval=5000;slider.style.width=`${slides.length * 100}%`;slides.forEach(slide=>{slide.style.width=`${100 / slides.length}%`});slides[0].classList.add('active');function showSlide(index){slider.style.transform=`translateX(-${index * (100 / slides.length)}%)`;slides.forEach((slide,i)=>{if(i===index){slide.classList.add('active')}else{slide.classList.remove('active')}});currentIndex=index}
function nextSlide(){currentIndex=(currentIndex+1)%slides.length;showSlide(currentIndex)}
function prevSlide(){currentIndex=(currentIndex-1+slides.length)%slides.length;showSlide(currentIndex)}
function startAutoSlide(){if(intervalId)clearInterval(intervalId);intervalId=setInterval(nextSlide,slideInterval)}
function stopAutoSlide(){clearInterval(intervalId)}
if(prevButton){prevButton.addEventListener('click',()=>{prevSlide();stopAutoSlide();startAutoSlide()})}
if(nextButton){nextButton.addEventListener('click',()=>{nextSlide();stopAutoSlide();startAutoSlide()})}
slidercontenedor.addEventListener('mouseenter',stopAutoSlide);slidercontenedor.addEventListener('mouseleave',startAutoSlide);let touchStartX=0;let touchEndX=0;slidercontenedor.addEventListener('touchstart',function(e){touchStartX=e.changedTouches[0].screenX;stopAutoSlide()},{passive:!0});slidercontenedor.addEventListener('touchend',function(e){touchEndX=e.changedTouches[0].screenX;handleSwipe();startAutoSlide()},{passive:!0});function handleSwipe(){const swipeThreshold=50;if(touchEndX<touchStartX-swipeThreshold){nextSlide()}else if(touchEndX>touchStartX+swipeThreshold){prevSlide()}}
showSlide(0);startAutoSlide()})}});document.addEventListener("DOMContentLoaded",()=>{const sliders=document.querySelectorAll(".slider-mini");sliders.forEach((slider)=>{const slides=Array.from(slider.querySelectorAll("li"));if(!slides.length)return;const prevBtn=slider.querySelector(".slider-mini-prev");const nextBtn=slider.querySelector(".slider-mini-next");let currentIndex=slides.findIndex(s=>s.classList.contains("active"));if(currentIndex<0)currentIndex=0;let autoplayId=null;const AUTOPLAY_MS=6000;function applyZ(){slides.forEach((s,i)=>{s.style.zIndex=s.classList.contains("active")?"2":"1"})}
function showSlide(index){index=(index+slides.length)%slides.length;slides.forEach((s,i)=>s.classList.toggle("active",i===index));currentIndex=index;applyZ()}
function nextSlide(){showSlide(currentIndex+1)}
function prevSlide(){showSlide(currentIndex-1)}
function startAutoplay(){stopAutoplay();autoplayId=setInterval(nextSlide,AUTOPLAY_MS)}
function stopAutoplay(){if(autoplayId){clearInterval(autoplayId);autoplayId=null}}
if(prevBtn)prevBtn.addEventListener("click",()=>{prevSlide();startAutoplay()});if(nextBtn)nextBtn.addEventListener("click",()=>{nextSlide();startAutoplay()});slider.addEventListener("mouseenter",stopAutoplay);slider.addEventListener("mouseleave",startAutoplay);slider.addEventListener("touchstart",stopAutoplay,{passive:!0});slider.addEventListener("touchend",()=>setTimeout(startAutoplay,250),{passive:!0});showSlide(currentIndex);startAutoplay()})});document.addEventListener('DOMContentLoaded',function(){const menuIcon=document.querySelector('.menu-icon');const navMenu=document.querySelector('.navbar-nav');document.querySelectorAll('.navbar-nav a').forEach(link=>{link.addEventListener('click',()=>{menuIcon?.classList.remove('active');navMenu?.classList.remove('active')})})});function updateCountdown(){const countdownElement=document.getElementById('countdown');const targetDate=new Date().getTime()+8*60*60*1000;function update(){const now=new Date().getTime();const distance=targetDate-now;const hours=Math.floor((distance%(1000*60*60*24))/(1000*60*60));const minutes=Math.floor((distance%(1000*60*60))/(1000*60));const seconds=Math.floor((distance%(1000*60))/1000);if(distance<0){clearInterval(interval);countdownElement.innerHTML="00:00:00"}}
update();const interval=setInterval(update,1000)}
document.addEventListener('DOMContentLoaded',updateCountdown);console.log("===== Este sitio web fue creado con TrummerWork, visita https://trummerwork-project.web.app/ para más información. Gracias por usar TRUMMERWORK. =====")